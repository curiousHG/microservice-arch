apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "architecture.fullname" . }}-test-connection"
  labels:
    {{- include "architecture.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "architecture.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
